/**
 * Layout Module
 *
 * Single source of truth for layout system.
 *
 * @example
 * ```typescript
 * import { LayoutService, BREAKPOINT_MD, getSafeAreaPadding } from '@core/layout';
 * ```
 */

// Tokens
export * from './tokens';

// Services
export * from './services';